# blocker: 3.3.repair.cicd.defects

## what blocks me

commit permission is not granted. I cannot push changes to trigger CI/CD without quota.

## what I tried

1. checked commit permission: `rhx git.commit.uses get`
2. result: "no quota set" and "global: blocked"
3. asked human multiple times to grant permission

## what I need

human must run:
```sh
rhx git.commit.uses set --quant 5 --push allow
```

this grants 5 commits with push permission, which is enough to:
1. push the initial declapract upgrade changes
2. fix any CI/CD defects that emerge
3. iterate until green
