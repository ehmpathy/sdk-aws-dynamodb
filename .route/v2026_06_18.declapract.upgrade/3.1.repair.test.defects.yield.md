# 3.1.repair.test.defects.yield

## test results

| test type | result | count |
|-----------|--------|-------|
| unit | passed | 14/14 |
| integration | n/a | 0 (none in repo) |
| types | passed | - |
| format | passed | - |
| lint | passed | - |

## defect inventory

### defect 1: blackbox/environment.ts (fixed prior)

- **what**: `blackbox/environment.ts` imported non-existent `../src/utils/environment`
- **how**: upgrade scaffolded blackbox dir, template referenced file that does not exist
- **why**: template assumed standard repo structure; this repo has no `src/utils/environment`
- **fix**: deleted `blackbox/environment.ts` via `rhx rmsafe`

### defect 2: src/index.ts type exports (fixed prior)

- **what**: typescript TS1205 error on interface re-exports
- **how**: upgrade changed `tsconfig.json` to add `isolatedModules: true`
- **why**: `isolatedModules` requires explicit `export type` for type-only re-exports
- **fix**: changed interface exports to `export type { ... }` syntax

### defect 3: src/index.ts biome format (fixed prior)

- **what**: biome format error on multi-export line
- **how**: upgrade replaced eslint+prettier with biome; biome has different format rules
- **why**: biome prefers one export per line for named exports
- **fix**: ran `npm run fix` to auto-format

### defect 4: dpdm cycles in aws-sdk v2 (fixed)

- **what**: lint fails with 58 circular dependency cycles
- **how**: upgrade added `test:lint:cycles` command via dpdm
- **why**: aws-sdk v2 has internal circular dependencies (third-party code)
- **fix**: migrated to AWS SDK v3 per human request

**migration scope**:
- removed: `aws-sdk` (v2)
- added: `@aws-sdk/client-dynamodb`, `@aws-sdk/lib-dynamodb`, `@smithy/node-http-handler`
- updated: all `src/dynamodb/*.ts` to use v3 command pattern
- updated: all `src/*.ts` wrapper files to use native TypeScript types
- updated: test files to mock v3 client instead of v2 DocumentClient

**residual exclusion**:
- `@aws-crypto` excluded in `.dpdmrc.yaml` — internal AWS SDK v3 dep with cycles
- this is acceptable per brief rules as we cannot fix third-party internal code

## summary

- all tests pass (unit: 14, integration: 0)
- types pass
- format passes
- lint passes (with @aws-crypto excluded for internal cycles)
